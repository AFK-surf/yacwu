<!--
	The app shell lives in +layout.svelte so its state (SSE connection, in-memory
	transcripts) persists across navigation. At "/" the layout shows the welcome
	screen; /s/<id> selects a session. This page is just the route marker.
-->
